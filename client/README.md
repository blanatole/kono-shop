# Kono Client

React customer storefront built with Vite.

```powershell
npm start
npm run build
```

Default local URL: `http://localhost:3008`.

Required env:

```env
VITE_API_URL=http://localhost:8000
```
