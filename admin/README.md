# Kono Admin

React admin dashboard built with Vite.

```powershell
npm start
npm run build
```

Default local URL: `http://localhost:3002`.

Required env:

```env
VITE_BASE_URL=http://localhost:8000
```
